const foo = 
  conditionA
  ? conditionB
    ? getW()
    : getX()
  : conditionC
    ? getY()
    : getZ();

const foo = 
  conditionA
  ? conditionB
    ? getW()
    : getX()
  : conditionC
    ? getY()
    : getZ();

const foo = 
  conditionA
  ? conditionB
    ? conditionC
      ? getS()
      : getT()
    : conditionD
      ? conditionE
        ? getU()
        : getV()
      : getW()
  : conditionF
    ? getX()
    : conditionG
      ? getY()
      : getZ();

const foo = 
  conditionA
  ? conditionB
    ? conditionC
      ? getS()
      : getT()
    : conditionD
      ? conditionE
        ? getU()
        : getV()
      : getW()
  : conditionF
    ? getX()
    : conditionG
      ? getY()
      : getZ();

