const condition = true;

const doX = () => {};
const doY = () => {};
const bar = (a, b, c, d, e) => {};

const foo = 
  condition
    ? doX()
    : doY();

const foo = 
  (
    condition
  )
    ? (
        doX()
      )
    : (
        doY()
      );

const foo = bar(
  1,
  2,
  condition
    ? doX()
    : doY(),
  3,
  4
);

const foo = 
  condition
    ? doX()
    : doY();

const foo = 
  condition
    ? doX()
    : doY();

const foo = 
  // Some comment
  condition
    ? doX()
    : doY();

const foo = 
  condition
    // Some comment
    ? doX()
    // Some
    // comment
    : doY();

const foo = 
  // a
  condition // b
    // c
    ? doX() // d // e // f
    // g
    : doY() // h // i // j
    // k;

const foo = /* a */ 
  condition /* b */
    /* c */
    ? doX() /* d */ /* e */
    /* f */
    : doY() /* g */ /* h */;

const foo = /* a */ 
  condition /* b */
    ? doX() /* c */ /* d */
    : doY() /* e */ /* f */;

