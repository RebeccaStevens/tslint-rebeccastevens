const foo = bar(
  condition
    ? getX()
    : getY(), 1, 2, 3, 4);

const foo = bar(
  condition
    ? getX()
    : getY(),
  1,
  2,
  3,
  4
);

const foo = bar(
  1,
  2,
  condition
    ? getX()
    : getY(),
  3,
  4
);

const foo = bar(
  1,
  2,
  3,
  4,
  condition
    ? getX()
    : getY()
);

