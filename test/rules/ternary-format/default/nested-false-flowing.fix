const foo = 
    conditionA
  ? getX()
  : conditionB
  ? getY()
  : getZ();

const foo = 
    conditionA
  ? getX()
  : conditionB
  ? getY()
  : getZ();

const foo = 
    conditionA
  ? getX()
  : conditionB
  ? getY()
  : getZ();

const foo = 
  conditionA
  ? getX()
  : conditionB
  ? getY()
  : getZ();

