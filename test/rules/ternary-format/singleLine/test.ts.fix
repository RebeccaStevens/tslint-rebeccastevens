const foo = 
  condition
    ? getX()
    : getY();

const foo = 
  (
    condition
  )
    ? (
        getX()
      )
    : (
        getY()
      );

const foo = 
  condition
    ? getX()
    : getY();

const foo = condition ? getX() : getY();

const foo = (condition) ? (getX()) : (getY());

