const foo = 
    conditionA
  ?   conditionB
    ?   conditionC
      ? doU()
      : doV()
    : conditionD
    ? doW()
    : doX()
  : conditionE
    ? doY()
    : doZ();

const foo = conditionA ? conditionB ? conditionC ? doU() : doV() : conditionD ? doW() : doX() : conditionE ? doY() : doZ();
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  [failure]

[failure]: Incorrectly formatted ternary operator.
