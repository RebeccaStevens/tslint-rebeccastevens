const condition = true;

const getX = () => {};
const getY = () => {};
const bar = (a, b, c, d, e) => {};

const foo = 
  condition
    ? getX()
    : getY();

const foo = 
  (
    condition
  )
    ? (
        getX()
      )
    : (
        getY()
      );

const foo = bar(
  condition
    ? getX()
    : getY(), 1, 2, 3, 4);

const foo = bar(
  condition
    ? getX()
    : getY(),
  1,
  2,
  3,
  4
);

const foo = bar(
  1,
  2,
  condition
    ? getX()
    : getY(),
  3,
  4
);

const foo = bar(
  1,
  2,
  3,
  4,
  condition
    ? getX()
    : getY()
);

const foo = 
  condition
    ? getX()
    : getY();

const foo = 
  condition
    ? getX()
    : getY();

const foo = 
  // Some comment
  condition
    ? getX()
    : getY();

const foo = 
  condition
    // Some comment
    ? getX()
    // Some
    // comment
    : getY();

const foo = 
  // a
  condition // b
    // c
    ? getX() // d // e // f
    // g
    : getY() // h // i // j
    // k;

const foo = /* a */ 
  condition /* b */
    /* c */
    ? getX() /* d */ /* e */
    /* f */
    : getY() /* g */ /* h */;

const foo = /* a */ 
  condition /* b */
    ? getX() /* c */ /* d */
    : getY() /* e */ /* f */;

