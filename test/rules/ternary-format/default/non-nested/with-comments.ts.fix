const foo = 
  // Some comment
  condition
    ? getX()
    : getY();

const foo = 
  condition
    // Some comment
    ? getX()
    // Some
    // comment
    : getY();

const foo = 
  // a
  condition // b
    // c
    ? // d
      // e
      getX() // f
    // g
    : // h
      // i
      getY() // j
    // k;

const foo = /* a */ 
  condition /* b */
    /* c */
    ? /* d */
      getX() /* e */
    /* f */
    : /* g */
      getY() /* h */;

const foo = /* a */ 
  condition /* b */
    ? /* c */
      getX() /* d */
    : /* e */
      getY() /* f */;

