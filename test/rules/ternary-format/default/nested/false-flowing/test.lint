const foo = 
    conditionA
  ? getX()
  : conditionB
  ? getY()
  : getZ();

const foo = conditionA ? getX() : conditionB ? getY() : getZ();
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

const foo = 
  conditionA
  ~~~~~~~~~~
    ? getX()
~~~~~~~~~~~~
    : conditionB
~~~~~~~~~~~~~~~~
        ? getY()
~~~~~~~~~~~~~~~~
        : getZ();
~~~~~~~~~~~~~~~~ [failure]

const foo = 
  conditionA
  ~~~~~~~~~~
  ? getX()
~~~~~~~~~~
  : conditionB
~~~~~~~~~~~~~~
  ? getY()
~~~~~~~~~~
  : getZ();
~~~~~~~~~~ [failure]

[failure]: Incorrectly formatted ternary operator.
