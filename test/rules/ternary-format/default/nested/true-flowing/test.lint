const foo = 
    conditionA
  ?   conditionB
    ? getX()
    : getY()
  : getZ();

const foo = conditionA ? conditionB ? getX() : getY() : getZ();
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  [failure]

[failure]: Incorrectly formatted ternary operator.
