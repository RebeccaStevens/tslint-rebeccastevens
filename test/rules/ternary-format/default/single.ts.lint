const condition = true;

const getX = () => {};
const getY = () => {};
const bar = (a, b, c, d, e) => {};

const foo = 
  condition
    ? getX()
    : getY();

const foo = 
  (
    condition
  )
    ? (
        getX()
      )
    : (
        getY()
      );

const foo = bar(condition ? getX() : getY(), 1, 2, 3, 4);
                ~~~~~~~~~~~~~~~~~~~~~~~~~~~               [failure]

const foo = bar(
  condition
    ? getX()
    : getY(),
  1,
  2,
  3,
  4
);

const foo = bar(
  1,
  2,
  condition
    ? getX()
    : getY(),
  3,
  4
);

const foo = bar(
  1,
  2,
  3,
  4,
  condition
    ? getX()
    : getY()
);

const foo = condition
            ~~~~~~~~~
  ? getX()
~~~~~~~~~~
  : getY();
~~~~~~~~~~  [failure]

const foo = condition ? getX() : getY();
            ~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

const foo = 
  // Some comment
  condition
    ? getX()
    : getY();

const foo = 
  condition
    // Some comment
    ? getX()
    // Some
    // comment
    : getY();

const foo = 
  // a
  condition // b
  ~~~~~~~~~~~~~~
    // c
~~~~~~~~
    ? // d
~~~~~~~~~~
    // e
~~~~~~~~
    getX() // f
~~~~~~~~~~~~~~~
    // g
~~~~~~~~
    : // h
~~~~~~~~~~
    // i
~~~~~~~~
    getY() // j
~~~~~~~~~~      [failure]
    // k;

const foo = /* a */ condition /* b */
                    ~~~~~~~~~~~~~~~~~
/* c */ ? /* d */ getX() /* e */
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
/* f */ : /* g */ getY() /* h */;
~~~~~~~~~~~~~~~~~~~~~~~~          [failure]

const foo = /* a */ condition /* b */ ? /* c */ getX() /* d */ : /* e */ getY() /* f */;
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~          [failure]

[failure]: Incorrectly formatted ternary operator.
