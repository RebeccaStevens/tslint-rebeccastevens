const condition = true;

const doX = () => {};
const doY = () => {};
const bar = (a, b, c, d, e) => {};

const foo = 
  condition
    ? doX()
    : doY();

const foo = 
  (
    condition
  )
    ? (
        doX()
      )
    : (
        doY()
      );

const foo = bar(
  1,
  2,
  condition
    ? doX()
    : doY(),
  3,
  4
);

const foo = condition
            ~~~~~~~~~
  ? doX()
~~~~~~~~~
  : doY();
~~~~~~~~~  [failure]

const foo = condition ? doX() : doY();
            ~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

const foo = 
  // Some comment
  condition
    ? doX()
    : doY();

const foo = 
  condition
    // Some comment
    ? doX()
    // Some
    // comment
    : doY();

const foo = 
  // a
  condition // b
  ~~~~~~~~~~~~~~
    // c
~~~~~~~~
    ? // d
~~~~~~~~~~
    // e
~~~~~~~~
    doX() // f
~~~~~~~~~~~~~~
    // g
~~~~~~~~
    : // h
~~~~~~~~~~
    // i
~~~~~~~~
    doY() // j
~~~~~~~~~      [failure]
    // k;

const foo = /* a */ condition /* b */
                    ~~~~~~~~~~~~~~~~~
/* c */ ? /* d */ doX() /* e */
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
/* f */ : /* g */ doY() /* h */;
~~~~~~~~~~~~~~~~~~~~~~~          [failure]

const foo = /* a */ condition /* b */ ? /* c */ doX() /* d */ : /* e */ doY() /* f */;
                    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~          [failure]

[failure]: Incorrectly formatted ternary operator.
